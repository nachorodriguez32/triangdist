# triangdist

Este es un paquete de R que implementa funciones para la distribución triangular (densidad, probabilidad acumulada, cuantiles y generación aleatoria).

## Instalación

Para instalar este paquete directamente desde GitHub, ejecuta el siguiente comando en tu consola de R:
```R
remotes::install_github("lutorval/triangdist")
